@namespace Moka.Beer
@inherits MokaComponentBase
<div style="@Style" class="@Class page @(Active ? "active" : "")">
    @ChildContent
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }

    [Parameter] public bool Active { get; set; } = true;
}